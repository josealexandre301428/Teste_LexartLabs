{
  "version": 2,
  "builds": [
    { "src": "frontend/build/**", "use": "@vercel/static" },
    { "src": "backend/**/*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "frontend/(.*)", "dest": "/" },
    { "src": "backend/(.*)", "dest": "/" }
  ]
}